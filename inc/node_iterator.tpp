#ifndef NODE_ITERATOR_T
# define NODE_ITERATOR_T

# ifndef NODE_ITERATOR_H
#  error __FILE__ should only be included via node_iterator.hpp
# endif

#include "containers.hpp" // DEBUG
#include <iostream> // for printer

namespace ft
{
	/*
	template<typename T>
	node_iterator<T>::node_iterator(void)
	{
		DEBUG_PRINT("ft::node_iterator : default constructor")
		_root = NULL;
		_current = NULL;
		_leaf = NULL;
		return ;
	}
	*/
};
#endif


